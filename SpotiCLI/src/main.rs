use reqwest;
use dotenv;

fn main() {
    Ok(dotenv); // loads .env
    let baseUrl = "https://api.spotify.com/v1/";
    println!("{}", baseUrl);
}

fn get_token()

fn search_album(){
    format!("https://api.spotify.com/v1/albums")
}